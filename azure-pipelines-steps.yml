steps:
- bash: |
    git clone https://github.com/asdf-vm/asdf.git
    . asdf/asdf.sh
    asdf plugin-test kubectl . 'kubectl version --client=true'
  displayName: asdf plugin-test
  env:
    GITHUB_API_TOKEN: $(GitHub.APIToken)
